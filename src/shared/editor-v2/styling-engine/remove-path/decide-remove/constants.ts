/** MD types whose HTML equivalent (b/i/s) can also wrap them — used for R9 trigger detection. */
export const MD_TYPES_WITH_HTML_EQUIVALENT: ReadonlySet<string> = new Set(['bold', 'italic', 'strikethrough']);
