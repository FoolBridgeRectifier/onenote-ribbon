/** Style attribute regex for span tag parsing. */
export const STYLE_ATTRIBUTE_REGEX = /style="([^"]+)"/;

/** Single CSS property:value pair pattern. */
export const STYLE_PROPERTY_VALUE_REGEX = /^\s*([^:]+?)\s*:\s*(.+?)\s*;?\s*$/;
