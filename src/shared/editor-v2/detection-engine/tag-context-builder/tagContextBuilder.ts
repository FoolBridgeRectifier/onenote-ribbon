// TODO: restore after engine refactor is complete (no longer called by DetectionEngine stub)
export function buildTagContextFromContent(content: string) {
  return { tags: [], protectedRanges: [], content };
}
