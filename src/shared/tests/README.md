# shared/tests

Unit tests for shared toggle utilities.

| File                       | Tests                    | Returns            |
| -------------------------- | ------------------------ | ------------------ |
| `toggleInline.unit.ts`     | T1-T3 inline wrap/unwrap | `{ test, pass }[]` |
| `toggleSubSup.unit.ts`     | T1-T3 sub/sup toggle     | `{ test, pass }[]` |
| `toggleLinePrefix.unit.ts` | T1-T4 prefix add/remove  | `{ test, pass }[]` |
