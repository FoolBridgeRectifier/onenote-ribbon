# insert-link/tests

Unit and edge-case tests for the Insert Link button.

- `insert-link.unit.ts` — verifies wrapping a selection and inserting an empty template.
- `insert-link.edge.ts` — verifies cursor positioning with and without a selection.
