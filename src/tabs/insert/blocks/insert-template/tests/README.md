# insert-template/tests

Unit and edge-case tests for the Insert Template button.

- `insert-template.unit.ts` — verifies button exists and does not throw on click.
- `insert-template.edge.ts` — verifies graceful handling when the templates plugin is not active.
