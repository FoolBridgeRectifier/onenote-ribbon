# insert-code-block/tests

Unit and edge-case tests for the Insert Code Block button.

- `insert-code-block.unit.ts` — verifies button exists, inserts two fences, and cursor is on line 1 ch 0.
- `insert-code-block.edge.ts` — verifies a second code block can be inserted after an existing one.
