# insert-table/tests

Unit and edge-case tests for the Insert Table button.

- `insert-table.unit.ts` — verifies button exists and inserts header row, separator, and data row.
- `insert-table.edge.ts` — tests mid-line insertion and insertion into an empty file.
