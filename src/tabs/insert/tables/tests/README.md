# tables/tests

Integration tests for the Tables group.

- `tables.integration.ts` — verifies the Tables group renders with the correct data-group attribute, group name, and contains the insert-table button.
