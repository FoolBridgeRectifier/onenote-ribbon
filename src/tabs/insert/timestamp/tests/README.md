# timestamp/tests

Integration tests for the Time Stamp group.

- `timestamp.integration.ts` — verifies the Time Stamp group renders with all three buttons: insert-date, insert-time, insert-datetime.
