# insert-date/tests

Unit and edge-case tests for the Insert Date button.

- `insert-date.unit.ts` — verifies button exists and inserts a YYYY-MM-DD formatted date.
- `insert-date.edge.ts` — tests moment.js path vs native Date fallback, and datetime combination.
