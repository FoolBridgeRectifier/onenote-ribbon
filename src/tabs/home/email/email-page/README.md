# EmailPageButton

Email Page button: copies page content to clipboard.

Reads the active editor's full content via `editor.getValue()` and writes it to `navigator.clipboard`. Shows a `Notice` on success or if no editor is active.
