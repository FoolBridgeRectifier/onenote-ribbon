# MeetingDetailsButton

Meeting Details button: inserts meeting YAML template.

On click, inserts a frontmatter block with `date`, `time`, `attendees`, `topics`, and `action-items` fields, followed by a `## Meeting Notes` heading, at the current cursor position.
