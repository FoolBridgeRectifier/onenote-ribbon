# todo-tag/

The large "To Do Tag" button that toggles `- [ ] ` prefix on the current line.

## Files

| File               | Purpose                                                                    |
| ------------------ | -------------------------------------------------------------------------- |
| `TodoTagButton.ts` | Renders the button and calls `toggleLinePrefix(editor, "- [ ] ")` on click |
