# font-family/tests/

Unit and edge tests for `FontFamilyPicker`.

- `font-family.unit.ts` — picker exists, label present, dropdown opens, contains Arial
- `font-family.edge.ts` — wraps selection in span; without selection sets vault config
