/** Number of columns in the bullet library grid. */
export const BULLET_LIBRARY_COLUMNS = 4;

/** Heading text rendered above the preset grid. */
export const BULLET_LIBRARY_HEADING = 'Bullet Library';

/** Pixel offset per nesting depth for the cascading level-symbol indent. */
export const BULLET_LIBRARY_INDENT_PER_LEVEL_PX = 2;
