# subscript/tests/

Unit and edge tests for `SubscriptButton`.

- `subscript.unit.ts` — wraps selection, strips tags on toggle off
- `subscript.edge.ts` — converts `<sup>` to `<sub>` when cursor is inside sup span; inserts empty pair at cursor
