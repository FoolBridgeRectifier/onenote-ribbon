/** Default highlight color applied when no custom color has been chosen. */
export const DEFAULT_HIGHLIGHT_COLOR = '#ffff00';

/** Default font color applied when no custom color has been chosen. */
export const DEFAULT_FONT_COLOR = '#ff0000';
