# bullet-list/tests/

Unit and edge tests for `BulletListButton`.

- `bullet-list.unit.ts` — adds/removes bullet prefix
- `bullet-list.edge.ts` — strips full checklist prefix variants, replaces heading prefix
