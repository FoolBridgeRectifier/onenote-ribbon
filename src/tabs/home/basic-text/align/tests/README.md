# align/tests/

Unit and edge tests for `AlignButton`.

- `align.unit.ts` — button exists, dropdown opens with 4 options, Left and Center labels present
- `align.edge.ts` — wraps selection with Left; wraps current line with Center when no selection
