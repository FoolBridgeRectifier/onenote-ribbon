# paste-dropdown/

Paste dropdown arrow module. Renders a small `onr-btn-sm` below the big Paste button showing a `▾` arrow.

On click, opens an overlay dropdown (via shared `showDropdown`) with three items: Paste, Paste as Plain Text, and Paste Special (disabled).
