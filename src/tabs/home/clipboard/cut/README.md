# cut/

Cut button module. Renders a small `onr-btn-sm` with scissors SVG and "Cut" label.

Gets the current editor selection, writes it to the clipboard, then replaces the selection with an empty string.
