# styles-dropdown/tests

Unit and edge-case tests for the `StylesDropdown` component.

## Files

- `styles-dropdown.unit.ts` — dropdown opens, 11 items present, "Heading 1" and "Normal" present.
- `styles-dropdown.edge.ts` — apply H2 on plain line, strip heading via Normal.
