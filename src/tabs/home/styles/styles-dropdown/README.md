# styles-dropdown

Static helper that opens the shared overlay dropdown populated with all 11 `STYLES_LIST` entries and their inline style previews.

## Files

- `StylesDropdown.ts` — `StylesDropdown.show(anchor, app)` delegates to `showDropdown` from the shared dropdown utility.
