# styles-scroll

Renders the up/down scroll buttons and the dropdown trigger button. Calls `setOffset` and triggers the `onUpdate` callback to refresh the preview.

## Files

- `StylesScroll.ts` — up/down clamp logic; dropdown button placeholder for `StylesDropdown`.
